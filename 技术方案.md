# Pic2Pet 技术方案

> 目标：参考 Petra 的 2.5D 桌宠核心能力，用 **Qt 6 + C++** 重写为**跨平台**桌面宠物应用，
> 在性能上明显优于 WebView 方案，并原生支持 **GIF / APNG / WebP / PNG 序列**。
> 文档只讲思路与结构，不含实现代码。

---

## 0. 结论先行

| 项 | 决策 |
|---|---|
| 语言 / 框架 | C++20 + Qt 6（LTS 线）+ CMake |
| UI 层 | **Qt Widgets**（表单式面板，无 QML 引擎开销） |
| 渲染层 | **原生 OpenGL**（QOpenGLWidget / QWindow + QOpenGLContext），不用 Scene Graph |
| 图像解码 | 自研 GifReader + libpng(自研 APNG 扩展) + libwebp，统一输出 `FrameSequence` |
| PSD | 自研只读解析器（8bit + RLE）+ 移植 rigger 装配算法 + `.petbin` 缓存 |
| 2.5D 运行时 | 1:1 移植 PsdRuntime（网格变形 + 弹簧物理 + stencil 虹膜裁剪） |
| 行为系统 | 移植 BehaviorEngine 状态机 |
| 不做的部分 | AI 助手、麻将游戏、日记/卡牌、歌词、音乐律动、自动更新（首版） |

跨平台支持矩阵：

| 平台 | 支持程度 | 说明 |
|---|---|---|
| Windows 10/11 | 完整 | 主目标平台 |
| macOS 12+ | 完整（需签名） | 未签名会被 Gatekeeper 拦截，需开发者账号做公证 |
| Linux X11 | 完整 | 依赖合成器（主流桌面均满足） |
| Linux Wayland | **降级** | 协议不允许普通窗口自定位/置顶，需 `wlr-layer-shell`（仅 wlroots 系合成器支持）|

---

## 1. 参考项目拆解：取什么、不取什么

### 1.1 Petra（Tauri 2 + WebView2 + Pixi.js）— 主要参考对象

| 模块 | 文件 | 规模 | 处置 |
|---|---|---|---|
| 2.5D 渲染运行时 | `src/live2d/psd/PsdRuntime.ts` | 695 行 | **移植核心**。WebGL 网格变形、发丝弹簧、胸弹、stencil 虹膜裁剪 |
| 驱动与表现层 | `src/live2d/psd/Rigged2DView.ts` | 538 行 | **部分移植**。取表情机、动作播放器、拖拽摆动、参数映射；去掉音乐驱动 |
| 装配算法 | `src/vendor/anime2dr/rigger.js` | 582 行 | **移植**。图层分类、连通域去噪、左右眼分离、发丝检测、锚点计算 |
| 通用部件 | `src/vendor/anime2dr/genericparts.js` | 43KB base64 | **导出为 PNG 资源**，运行时读取 |
| 动作定义 | `src/live2d/actions.ts` | 197 行 | **移植**。14 个动作的关键帧表 + 采样器 |
| 行为引擎 | `src/autonomous/BehaviorEngine.ts` | 602 行 | **移植**。漫游/躲避/追踪/待机/边界约束/模型偏移补偿 |
| 穿透调度 | `src-tauri/src/lib.rs` 的 `spawn_clickthrough_watcher` | ~70 行 | **移植思路**。16ms 轮询光标 + 矩形区域命中 → 动态切换穿透 |
| 主装配与 UI | `src/main.ts` | 3127 行 | **不取**。菜单、面板、模型管理、公告、日志埋点 |
| AI 助手 | `AssistantClient/Panel` | 1627 行 | **不取** |
| 麻将 / 日记 / 卡牌 / 歌词 / 音频 | 多文件 | ~3000 行 | **不取** |
| Rust 后端其余部分 | `lib.rs` 2132 行 | | **只取**：穿透调度、光标/工作区查询、DPAPI 不需要 |

### 1.2 desktop-pet2（Swift/AppKit + C++17/Win32）— 性能范式参考

| 模块 | 处置 |
|---|---|
| 帧序列结构 `FrameSequence` | **取**。帧数组 + 延迟数组 + `frameIndexAt(t)`，直接照搬概念 |
| `AnimationPlayer` 的帧调度 | **取**。`idx == lastFrameIndex` 时不重绘（关键省电设计） |
| 加载时全量预解码 | **取**。这是它做到"待机 0% CPU"的根本原因 |
| GIF disposal 处理 | **取**。disposal=2 清空区域、=3 恢复备份，这是残影 bug 的唯一来源 |
| Win32 分层窗口实现 | **只参考思路**，跨平台不能依赖 WIC/Win32 |
| WIC 解码器 | **不取**，Windows 专属 |

### 1.3 desktop-pet1（Electron）— 仅参考概念

GPL-3.0 许可，存在传染性，**不参考其任何代码**。只借鉴"素材包 + 规则引擎"的产品概念，
本方案首版不做规则引擎。

---

## 2. 总体架构

```
                        ┌─────────────────────────────┐
                        │   App (QApplication)        │
                        │   单实例 / 托盘 / 生命周期   │
                        └──────────────┬──────────────┘
                                       │
        ┌──────────────────────────────┼──────────────────────────────┐
        │                              │                              │
┌───────▼────────┐          ┌──────────▼──────────┐        ┌─────────▼────────┐
│  PetWindow     │          │   BehaviorEngine    │        │  SettingsPanel   │
│  (平台窗口层)   │◄─────────┤   行为状态机         │        │  (Qt Widgets)    │
│  透明/置顶/穿透 │  位置/状态 │   漫游·躲避·追踪    │        │  模型/行为/显示   │
└───────┬────────┘          └──────────┬──────────┘        └─────────┬────────┘
        │                              │                              │
        │                     ┌────────▼──────────┐                   │
        │                     │   PetDriver       │◄──────────────────┘
        │                     │  参数聚合器        │   用户覆盖参数
        │                     │  表情机/动作机     │
        │                     └────────┬──────────┘
        │                              │ RigParams (36 通道)
┌───────▼──────────────────────────────▼──────────────────────────────────────┐
│                        Renderer (OpenGL 后端)                                │
│   ┌────────────────────────┐        ┌──────────────────────────────────┐    │
│   │ SpritePass             │        │ RigPass                          │    │
│   │ 单 quad + 纹理          │        │ N 层三角网格 + 逐顶点变形         │    │
│   │ (GIF/APNG/WebP/序列)   │        │ stencil 虹膜裁剪 / 弹簧物理       │    │
│   └────────────────────────┘        └──────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────────┘
        ▲                              ▲
        │ FrameSequence                │ Rig (图层 + 锚点 + 发丝)
┌───────┴────────────┐        ┌────────┴──────────────────────┐
│  media/            │        │  psd/                          │
│  GifReader         │        │  PsdReader (自研只读)           │
│  PngApngReader     │        │  Rigger (移植)                  │
│  WebpReader        │        │  PetBin (.petbin 读写/缓存)      │
│  PngSequenceReader │        │                                 │
└────────────────────┘        └─────────────────────────────────┘
```

数据流一句话：**素材 → 解码/装配 → 常驻内存的数据结构 → 每帧只做"参数计算 + 顶点变形 + 一次绘制"**，
播放期零解码、零 IO、零分配。

---

## 3. 关键技术决策

### 3.1 为什么是 Qt Widgets 而不是 Qt Quick

| 维度 | Widgets | Quick (QML) |
|---|---|---|
| 内存基线 | 小（无 QML 引擎/JIT） | +15~25MB |
| 冷启动 | 快 | 慢（需加载 QML 引擎、编译 QML） |
| 表单式设置面板 | 天然契合（QFormLayout） | 需手写 |
| 二进制体积 | 小（不链接 QtQuick） | 大 |

桌宠主体是**纯渲染窗口**，不需要任何标准控件；设置面板是**表单**。Widgets 完全够用且更省。
外观用 QSS 处理圆角/配色即可。

### 3.2 为什么是原生 OpenGL 而不是 Scene Graph / QPainter

这是本方案最重要的决策，理由有三：

1. **stencil 可用**。Petra 的虹膜裁剪依赖 stencil buffer（眼白写入模板 → 虹膜按模板测试）。
   Qt Quick 的 Scene Graph 不暴露 stencil，改造成本高；原生 OpenGL 只要在
   `QSurfaceFormat` 里设 `setStencilBufferSize(8)` 就能 1:1 照搬。
2. **逐顶点变形可控**。RigPass 每帧要更新 VBO（几十层的顶点数组），原生 GL 直接
   `bufferData(DYNAMIC_DRAW)`，与 WebGL 版本逻辑一致，移植风险最低。
3. **shader 直接复用**。PsdRuntime 的 vertex/fragment shader 只有几行，可直接搬过来。

QPainter 直接排除：无法逐顶点变形，且每帧 CPU 光栅化不达标。

### 3.3 为什么自研解码而不是用 Qt 的 QImageReader

`QImageReader` 能读静态图和 GIF，但：动画帧的延迟/disposal 元数据 API 弱且各平台插件行为不一致，
APNG 完全不支持。**为了三平台行为一致**，解码层自己掌控，Qt 只负责窗口与 UI。

### 3.4 为什么自研 PSD 解析器

C++ 生态没有 ag-psd 的等价物（libpsd 老旧、不支持现代 PSD 变体）。
而需求其实很窄：**只读、8bit、RLE 压缩、扁平图层**（rigger 本身就要求扁平、不支持图层组）。
自研一个 1200~2000 行的只读解析器完全可控，且无第三方许可负担。

配套两条保险：
- 明确的支持范围 + 友好的报错文案（"该 PSD 为 16bit，请用 8bit 导出"）
- `.petbin` 缓存：装配一次后落盘，后续启动不再碰 PSD

---

## 4. 模块设计

### 4.1 平台窗口层（`platform/`）

对外统一接口，内部按平台实现：

```
IPetWindow
  create(x, y, w, h)
  setAlwaysOnTop(bool)
  setClickThrough(bool)        // 动态切换，必须无闪烁
  setOpacity(float)
  setPosition / setSize
  screenWorkAreaAt(x, y)       // 多屏：返回光标所在屏的工作区
  cursorScreenPos()
```

| 能力 | Windows | macOS | Linux X11 |
|---|---|---|---|
| 无边框透明 | `WS_EX_LAYERED` / DWM 合成 | `NSWindow` opaque=NO + clearColor | ARGB visual + 合成器 |
| 置顶 | `HWND_TOPMOST`（需看门狗，会被其他全屏窗口抢走） | `NSStatusWindowLevel`+ | `_NET_WM_STATE_ABOVE` |
| 鼠标穿透 | `WS_EX_TRANSPARENT` | `setIgnoresMouseEvents:` | XShape 输入区域 |
| 不在任务栏 | `WS_EX_TOOLWINDOW` | `NSApplicationActivationPolicyAccessory` | skipTaskbar hint |

**Qt 优先，原生兜底**：MVP 先全部用 Qt 的 `Qt::FramelessWindowHint` / `Qt::WindowStaysOnTopHint` /
`Qt::WindowTransparentForInput` 实现；若某平台出现"切换穿透导致窗口重建闪烁"或"透明合成 CPU 过高"，
再在该平台下沉到原生实现。接口隔离好了，替换不影响上层。

**穿透调度（关键机制，移植自 Petra）**：
```
后台线程 16ms 轮询：
  cursor = 光标屏幕坐标
  hit = 角色包围盒命中测试(用 alpha mask 或矩形区域列表)
  setClickThrough(!hit && !dragging && !menuOpen)
```
- 命中区域用**矩形列表**（Petra 做法，最多 128 个，支持并集），比逐像素 mask 便宜且够用
- 拖拽中 / 菜单打开时强制不穿透
- 只在状态**发生变化**时调用平台接口，避免每 16ms 触发窗口重建

### 4.2 素材解码与帧序列（`media/`）

统一产物：
```
FrameSequence {
    vector<Frame> frames;   // Frame = { w, h, stride, RGBA8 数据 }
    vector<double> delays;  // 秒
    double totalDuration;
    int frameIndexAt(double t);
}
```

**解码策略（照搬 desktop-pet2，这是性能的根）**：
- 加载时**一次性全解码**为 RGBA8 常驻内存，播放期不做任何解码
- 帧调度器：`idx == lastFrameIndex` 则**直接返回，不重绘**（24fps 素材在 120Hz 屏上跳过 5/6 次回调）
- 内存预算：典型桌宠 < 100 帧 × 512² × 4B ≈ 100MB 上限；超过阈值时提示并对超大素材做降采样

**GIF 必须正确处理**（残影的唯一来源）：
- 全局/局部调色板、透明色索引
- `Graphic Control Extension` 的 delay / disposal / transparent flag
- disposal = 0/1 保留；= 2 绘制后清空该帧矩形；= 3 恢复为绘制前备份
- delay ≤ 1cs 的畸形值兜底为 10cs

**APNG**：标准 PNG 解析基础上，处理 `acTL`（帧数/循环）、`fcTL`（每帧控制，含 delay_num/den、dispose_op、blend_op）、`fdAT`（帧数据）。默认首帧是普通 IDAT。

**格式优先级**：GIF → APNG → WebP(动) → PNG 序列（目录，按文件名排序）→ 静态图（单帧）。

### 4.3 PSD 解析与装配（`psd/`）

**阶段一：PsdReader（自研只读）**
- 解析顺序：文件头 → 颜色模式数据 → 图像资源 → 图层与掩码信息 → 各图层图像数据
- 支持：8bit、RGB/灰度/RGBA、**RLE 压缩**（PSD 默认）
- 明确不支持并友好报错：16/32bit、CMYK/LAB、图层组（要求先扁平化）、ZIP 压缩（后期可加）
- 输出：`vector<PsdLayer>{ name, left, top, w, h, RGBA8 }`

**阶段二：Rigger（移植 rigger.js）** — 纯几何 + 图像算法，无外部依赖：

| 算法 | 说明 | 移植注意 |
|---|---|---|
| `normName` | 图层名归一化（含 see-through 输出的 `mouth` → `mouth_open` 别名） | 需 Unicode NFKC，Qt 自带 |
| `SLOTS` 表 | 17 个语义槽位，每个带 depth/group/phys/fade/split 属性 | 原样搬 |
| `labelComponents` | 全画布 alpha 连通域标记（非递归栈式 flood fill） | 768² 画布需 `Int32Array` 级别内存，C++ 用 `vector<int32_t>` |
| `cleanAlpha` | 去掉 < 40px 的碎块 + 膨胀 3px | |
| `splitSides` | 按连通域质心 vs 脸部中心 x 分左右 | 用于 eyewhite/irides/eyelash/eyebrow/eye_close |
| `detectStrands` | 每列取最下沿 → 41 点 box 平滑 → 峰值检测（prominence ≥ 10）→ 取 2~6 束 | 发丝物理的输入 |
| `findPeaks` | 带最小间距与显著度的峰值筛选 | |
| 锚点计算 | face bbox/质心、eyeL/R（bbox + 虹膜质心 + closeY）、mouth、neckPivot、bodyPivot、`faceScale = 脸宽 / 333` | 数值必须逐位对齐，否则手感会变 |
| 通用部件合成 | 缺 eye_close / mouth_close 时，用内置 PNG 缩放 + 取色重着色 + 定位插入 | genericparts 导出为 PNG 资源 |

> **一致性验证方法**：用同一份 PSD，分别跑 JS 版与 C++ 版，导出锚点/部件 bbox/strand 坐标为 JSON 做逐字段比对。
> 这是移植阶段最重要的回归测试，必须自动化。

**阶段三：`.petbin` 缓存**
```
"PETB" + version
canvas: w, h
anchors: face / eyeL / eyeR / mouth / neckPivot / neckTop / neckBottom / bodyPivot / faceScale / hairRootY
layerCount
per layer:
    name, x, y, w, h, depth, group, fade, side
    strandCount, strands[] {x, tipY, rootY}
    pixels (zstd 压缩 RGBA8)
```
导入 PSD 时装配一次 → 写 `.petbin` → 后续启动直接读（秒开，且完全绕开 PSD 解析）。
同时给一个"导出 .petbin"功能，方便分享角色。

### 4.4 2.5D 渲染运行时（`render/` + `rig/`）

网格构建（装配时一次）：
- 每层按 `cell ≈ 30~42px`（物理层 30，普通层 42，按 `CW/768` 缩放）切成 `nx × ny` 网格
- 生成：base 顶点（画布坐标）、UV、索引、纹理
- 发丝层额外预计算：每顶点对每个 strand 的高斯权重 `sw`、归一化高度 `su`、刘海三束权重 `bw`

每帧（`RigPass`）：
1. `BehaviorEngine` + `Driver` → 36 通道 `RigParams`
2. 参数平滑：`cur += (tgt - cur) * min(1, dt*14)`
3. 发丝弹簧（stiff/soft 双弹簧，k=70/16, c=9/1.3）、胸弹（k=140, c=4.2）
4. 逐层 `deform()`：眼/嘴/眉的局部变形 → 头部绕颈枢旋转（按 depth 加权）→ 身体摆动 → 呼吸 → 发丝位移
5. 上传 VBO → 按 z 顺序绘制；`eyewhite` 写 stencil，`irides` 按 stencil 测试绘制（虹膜不出眼白）

**顶点量级评估**：30 层 × 平均 ~150 顶点 ≈ 4500 顶点/帧。C++ 下变形计算 < 0.5ms，远低于 16.7ms 预算。
这也是为什么帧率不会比 WebGL 版本更高——**真正的收益在内存、启动和帧时间抖动**。

**渲染优化点**：
- 静态（低活动档 + 无交互）时降到 30fps
- 窗口完全被遮挡 / 最小化 / 隐藏时停止渲染循环
- 只在 `RigParams` 实际变化或需要动画时 `update()`

### 4.5 驱动与行为（`rig/` + `behavior/`）

`RigParams` 为 36 字段的 POD 结构体，`Driver` 每帧聚合四路输入：

| 来源 | 内容 |
|---|---|
| 行为引擎 | `vx`（横向速度）、`bob`（走动起伏）、`cursorDx/Dy`（视线，`flip` 支持待机倒挂） |
| 表情机 | 10 个预设表情（微笑/惊讶/眯眼/委屈/生气/害羞/左右眨眼/哈欠/好奇），`sin(π·progress)` 包络淡入淡出 |
| 动作机 | 14 个动作（点头/摇头/歪头/挥手/打哈欠/伸懒腰…），关键帧采样 + 0.2s 首尾淡入淡出，按活动频率自动播放 |
| 交互 | 按压眯眼、拖拽下半身摆动（弹簧）、点击脉冲、拖拽惯性 |

`BehaviorEngine` 状态机（移植，~700 行）：
```
Roam      — 在工作区内随机取点，按档位速度移动，到达后按 restChance 休息
Flee      — 光标进入 165px 半径 → 背离光标逃跑 260px，持续 900~1500ms
Chase     — 逗猫棒模式：追光标，目标点夹在"模型完整可见"范围内
Idle      — 沉到就近屏幕边缘（上半屏→顶部倒挂 180°），完全静止
DragSuspend — 拖拽中暂停一切自主行为
LowActivity — 低档：原地静止，仅保留呼吸/眨眼/视线
```
活动三档参数（restBase / restChance / radiusDiv / speed）：
低 = `600000ms / 0.95 / 8 / 12`；中 = `8000 / 0.3 / 3.5 / 55`；高 = `1500 / 0.05 / 2 / 120`。

**模型偏移补偿**：窗口可以探出屏幕，但角色不能。每帧用角色包围盒反算窗口内偏移，
使角色始终留在工作区内（平滑系数 0.3，避免抖动）。这是 Petra 手感的关键细节，必须一起移植。

### 4.6 设置与持久化（`app/`）

- `QSettings`（Ini 格式，路径 `QStandardPaths::AppLocalDataLocation`）跨平台统一
- 每只宠物独立配置：素材路径、缩放、透明度、速度、翻转、置顶、锁定位置、活动档位
- 多宠物：一个 `PetInstance` 一个窗口，各自独立配置（首版限制 8 只）
- 开机自启：Windows 注册表 Run / macOS LaunchAgent / Linux `.desktop` autostart
- 系统托盘：`QSystemTrayIcon`（三平台一致）+ 右键菜单

---

## 5. 性能目标

> **2026-09-18 修订**：内存目标由 <60MB 放宽。M0 实测 Qt 模块基线就是 70MB，
> 为压内存而把 `QOpenGLWidget` 换成 `QWindow`+`QOpenGLContext` 的代价（拖拽/菜单全部自绘）
> 不值得。**保留 QtWidgets**，改为控制"素材帧内存"这一可控部分。
> 相比 Petra 单只 120~250MB，本方案仍有数量级优势。

| 指标 | 目标（修订后） | 手段 |
|---|---|---|
| 空转内存（Qt 基线） | **≤ 80MB**（实测 ~70MB，接受） | 无 WebView、无 QML、无 Chromium |
| 单只宠物（典型 GIF 60帧 320²） | **≤ 120MB** | 帧内存 ≈ 24MB |
| 帧内存硬上限 | **150MB**，超过则提示并降采样 | 加载时预检 + 自动缩放 |
| 空闲 CPU | ≈ 0% | 静态图加载后直接停定时器 |
| 播放 CPU（24fps 素材） | **< 1.5%** | 预解码 + 帧未变则跳过 + 定时器自适应 |
| 2.5D 帧时间 | < 2ms | 顶点变形在 C++ 侧，无 GC |
| 2.5D 帧时间 | < 2ms | 顶点变形在 C++ 侧，无 GC |
| 冷启动 | < 300ms | 无运行时初始化；`.petbin` 直读 |
| 安装包 | Windows ~25MB | windeployqt 只带 Qt Core/Gui/Widgets/OpenGL |

对比基线：Petra（WebView2 常驻 120~250MB、启动 1~2s）。

---

## 6. 目录结构与构建

```
Pic2Pet/
├─ CMakeLists.txt
├─ src/
│  ├─ app/        入口、单实例、托盘、配置、宠物实例管理
│  ├─ platform/   IPetWindow 接口 + win/ mac/ x11/ 实现 + 穿透调度线程
│  ├─ render/     GL 后端、SpritePass、RigPass、Shader、纹理管理
│  ├─ media/      GifReader / PngApngReader / WebpReader / PngSequenceReader
│  ├─ psd/        PsdReader / Rigger / PetBin
│  ├─ rig/        RigParams、deform、弹簧物理、表情表、动作表
│  ├─ behavior/   BehaviorEngine 状态机
│  └─ ui/         Qt Widgets：设置面板、模型管理、托盘菜单
├─ thirdparty/    libpng / libwebp / zlib / zstd（vcpkg 管理）
├─ resources/     内置模型 .petbin、genericparts PNG、图标
├─ tools/         rigger 一致性比对脚本、PSD→petbin 离线转换工具
└─ docs/
```

构建：CMake + vcpkg（三平台一致）。
Windows: MSVC 2022 + Qt 6（MSVC 版）；macOS: Xcode clang；Linux: GCC/Clang。
本机现有 MSYS2 UCRT64，可直接 `pacman -S mingw-w64-ucrt-x86_64-qt6` 起 Windows 开发。

打包：Windows `windeployqt` + NSIS；macOS `macdeployqt` + codesign/notarize + dmg；
Linux AppImage（linuxdeploy）。

---

## 7. 实施计划

| 里程碑 | 内容 | 产出与验收 |
|---|---|---|
| **M0 Spike** | 三平台：透明置顶窗口 + OpenGL 绘制 + 穿透动态切换 + 拖拽 | 实测内存/CPU；**这是决定架构能否成立的关口** |
| **M1 帧播放 MVP** | GIF/APNG/PNG序列/静态图解码 + 预解码播放 + 托盘 + 设置 + 开机自启 | 可直接发布的"GIF 桌宠"：单只 ≤ 120MB，静态图 CPU ≈ 0，24fps 播放 CPU < 1.5% |
| **M2 2.5D 核心** | PsdReader + Rigger 移植 + RigPass + `.petbin` | 同一 PSD 与 JS 版锚点逐字段一致；导入 → 桌宠秒开 |
| **M3 行为与互动** | BehaviorEngine + Driver + 表情/动作 + 多宠物 | 手感对齐 Petra |
| **M4 打磨发布** | 多屏/DPI、开机自启、三平台打包、日志 | 三平台可安装 |

M1 结束即可对外发布一版，M2 是差异化能力。

---

## 8. 风险登记册

| # | 风险 | 概率 | 影响 | 应对 |
|---|---|---|---|---|
| 1 | Windows 上 Qt 透明窗口走 layered 路径，导致每帧 CPU 合成、吃掉 GPU 收益 | 中 | 高 | **M0 必须实测**；不达标则窗口层下沉原生 Win32（参考 desktop-pet2 的 304 行实现） |
| 2 | Wayland 下无法自定位/置顶 | 高 | 中 | 明确降级说明；X11 会话完整支持；后期可加 `wlr-layer-shell` |
| 3 | 自研 PSD 解析器兼容性不足 | 中 | 中 | 限定 8bit+RLE+扁平；友好报错并给出转换指引；`.petbin` 旁路 |
| 4 | Rigger 移植后锚点漂移，手感变形 | 中 | 高 | 自动化 JSON 比对测试（tools/），逐字段校验 |
| 5 | 动态切换穿透导致窗口重建/闪烁 | 中 | 中 | 只在状态变化时切换；不行则下沉原生接口 |
| 6 | macOS OpenGL 已弃用 | 低 | 中 | macOS 仍可用；后期可评估切 Qt RHI（Metal），成本较高 |
| 7 | 打包体积超标 | 低 | 低 | 只链接必需 Qt 模块；静态链接 zstd/libpng |
| 8 | Qt 6 LGPL 合规 | — | 中 | 动态链接 Qt、随附 LGPL 声明与 Qt 源码获取方式；或改用商业许可 |

**许可合规清单**（移植代码均需保留署名）：Petra (MIT)、Anime2.5DRig (MIT)、ag-psd (MIT)、
desktop-pet2 (MIT)。desktop-pet1 为 GPL-3.0，**不参考其任何代码**。

---

## 9. 端到端使用链路（对齐"一张图片直出桌宠"）

```
照片/立绘
   │  see-through（外部：HuggingFace / ModelScope 在线 Demo，或本地部署）
   ▼
分层 PSD（face / eyewhite / irides / front hair / ...）
   │  拖入 Pic2Pet（或菜单导入）
   ▼
PsdReader → Rigger 自动装配 → .petbin 缓存
   │
   ▼
2.5D 桌宠：眨眼 / 视线跟随 / 发丝物理 / 呼吸 / 14 个动作 / 桌面漫游 / 躲避鼠标
```

首版不内置 see-through（模型大、部署重），沿用外部工具拆分，与 Petra 一致；
仅在使用手册里给出推荐流程和图层命名规范。

---

## 10. 需要在 M0 验证的问题（结论决定是否调整架构）

1. Windows/macOS/Linux 上，Qt 透明窗口 + OpenGL 内容能否正确合成且 CPU 可接受？
2. `Qt::WindowTransparentForInput` 动态切换是否会导致窗口重建或闪烁？
3. 高 DPI（150%/200%）下坐标换算与多屏工作区获取是否一致？
4. 三平台 OpenGL 上下文创建成功率（尤其是老旧核显 / 虚拟机）？
5. 隐藏/最小化后能否可靠停止渲染循环（避免后台占用）？

---

## 11. M0 spike 实测结果（2026-09-18，Windows 已验证）

**环境**：Windows 11（build 26200）· MSVC 19.44.35211 · Qt 6.11.2 msvc2022_64 · CMake 4.4.3 · Ninja 1.13.2
**二进制**：`Pic2Pet.exe` **73.5 KB**（Release，静态链接 MSVC 运行时，不含 Qt DLL）

### 11.1 实测数据

| 指标 | native 后端（WS_EX_TRANSPARENT） | Qt 后端（WindowTransparentForInput） | 目标 |
|---|---|---|---|
| timerFps | 62–63 | 62–63 | ≥60 |
| renderFps | 62–64 | 62–64 | ≥60 |
| CPU（60fps 持续动画） | 1.6%–10.9% | 1.6%–7.8% | <1%（待机） |
| 常驻内存 | 70–72 MB | 70–77 MB | **<60 MB ❌** |
| 穿透切换次数（9 秒内） | 1（稳定） | 1→2（有一次抖动） | 稳定 |
| GL 上下文 | desktop OpenGL 4.6.0 / NVIDIA RTX 4060 Laptop GPU | 同 | — |

### 11.2 逐条回答第 10 节的问题

1. **透明窗口 + OpenGL 合成**：✅ 通过。拿到的是**原生 desktop OpenGL 4.6**（NVIDIA 驱动，未回落 ANGLE/软件渲染），60fps 稳定，无掉帧。**"Windows layered 合成吃掉 GPU 收益"这一最大风险在此机器上未发生。**
2. **`WindowTransparentForInput` 动态切换**：⚠️ 基本可用但不干净。9 秒内出现了第 2 次 toggle，说明存在一次非预期的状态翻转；而 `WS_EX_TRANSPARENT` 全程只有 1 次 toggle。
   → **结论：Windows 上默认走 Win32 原生后端**，`Qt::WindowTransparentForInput` 只作为非 Windows 平台的通用兜底。
3. **高 DPI**：✅ 实测 dpr=1.50，屏幕 1707×1067（逻辑像素），`interactiveRect()` 得到 224×224 命中区，坐标换算正确。
4. **OpenGL 上下文创建**：✅ 本机成功。但 **macOS（OpenGL 已废弃）和 Wayland 仍未验证**，需要在 M1 前补测。
5. **停止渲染循环**：❌ 未验证（本次 spike 未实现隐藏态，也未提供自动退出机制）。

### 11.3 唯一超标项：内存 70 MB

spike **不加载任何图片资源**，70 MB 全是 Qt 模块本身的常驻成本：
Qt6Core + Qt6Gui + Qt6Widgets + Qt6OpenGL + Qt6OpenGLWidgets + NVIDIA GL 驱动。

目标写的是 <60 MB，照当前的模块组合**达不到**。可选对策（按收益排序）：

- **去掉 QtWidgets，改用 `QWindow` + `QOpenGLContext`**（面板用单独的原生/轻量窗口）。Widgets 是最大的一块，预计可省 15–25 MB。代价：PetWindow 的拖拽/菜单要自己处理（本次 spike 已经自己处理了拖拽，菜单用 `QMenu` 仍属 Widgets，需改为自绘或只在需要时动态拉起 Widgets 窗口）。
- 面板窗口**按需创建、用完销毁**，不常驻。
- 延迟加载：OpenGL 上下文在首帧需要时才创建。

> 建议：M1 阶段把 `PetWindow` 从 `QOpenGLWidget`（Widgets）迁移到 `QWindow` + `QOpenGLContext`（Gui），顺带解决内存与 Wayland 兼容性两个问题。设置面板保留 QtWidgets，但做成独立进程或按需加载。

### 11.4 CPU 说明

实测 CPU 是**持续 60fps 动画 + 16ms 光标轮询**下的占用，不是待机值。要拿到 <1% 的待机数字，还需在 M1 落实：
- 帧未变化则跳过重绘（对齐 desktop-pet2 的 `idx == lastFrameIndex` 优化）
- 光标轮询降到 30–50ms，或改为事件驱动
- 窗口不可见时停掉所有定时器

### 11.5 构建环境的坑（复盘，供后续参考）

1. **CMake 会误选 MSYS2 的 g++**（PATH 里 `D:/program/msys64/ucrt64/bin` 靠前）。必须显式指定 `CMAKE_CXX_COMPILER` 指向 MSVC 的 `cl.exe`。
2. **不要用 `-G "Visual Studio 17 2022"`**：本机进程环境块里同时存在 `Path` 和 `PATH`，MSBuild 的 ToolTask 建 Hashtable 时必抛 `MSB6001`。Ninja 不受影响。
3. **PowerShell 里 `-DXXX=$var` 形式的实参变量不展开**（会被原样传成字符串 `$var`）。必须写成 `"-DXXX=$var"`（带引号）。
4. **换生成器前必须删掉 `build/`**，否则报 `Does not match the generator used previously`。
5. Qt 的 `qInfo()` 在 Windows 默认不走控制台，需设 `QT_LOGGING_TO_CONSOLE=1`（已废弃，建议改 `QT_FORCE_STDERR_LOGGING=1`）。

### 11.6 下一步（M1）

1. ~~把 `PetWindow` 迁移到 `QWindow` + `QOpenGLContext` 压内存~~
   → **取消**（2026-09-18 用户决策：内存可以放宽，不为它动架构，保留 QtWidgets）
2. 落实"帧未变化跳过重绘" + 静态图停定时器，拿待机 CPU 数据 —— **仍是 M1 必做**
3. Windows 穿透默认锁定 Win32 原生后端 —— **仍是 M1 必做**
4. ~~补测 macOS / Linux~~ → **取消**（用户决策：不做实测，只保证代码层可编译与接口抽象）

---

## 12. M1 实施规划：帧播放 MVP

### 12.0 范围与红线

**目标产物**：一个能直接发出去的"GIF 桌宠"——加载本地动图/序列/静态图，在桌面循环播放，
可拖动、可穿透点击、可缩放/调透明度/置顶/开机自启/退出。

**做**：
- 素材解码：GIF（动）、APNG（动）、静态图（PNG/JPG/BMP…）、PNG 序列（目录）
- 预解码播放 + 帧调度 + 三重短路（省 CPU）
- 托盘菜单 + 右键菜单 + 拖拽加载 + 配置持久化 + 开机自启

**不做**（留给 M2/M3）：
PSD 解析、rig 装配、2.5D 变形、行为状态机、多宠物、表情/动作、音乐律动、多屏策略

**平台策略**（用户决策）：
只保证 **Windows 实测通过**；macOS / Linux **只做代码层兼容**——
所有平台相关代码走 `#ifdef Q_OS_*` 隔离并编译通过，接口抽象到位，但**不做运行验证**。
跨平台风险（Wayland 自定位/置顶、macOS OpenGL 废弃）在文档里标注为"已知未验证"。

### 12.1 解码层设计（`src/media/`）

**零第三方依赖策略**（省掉 vcpkg，M1 不引入任何外部库）：

| 格式 | 方案 | 理由 |
|---|---|---|
| **GIF（动）** | **自研**（LZW + 调色板 + disposal） | 核心需求，必须可控；QImage 不支持动 GIF |
| **APNG（动）** | **自研 chunk 分割 + QImage 解码** | Qt 只认首帧；把每帧 `fdAT` 拼成完整 PNG 交给 QImage |
| 静态图 | `QImage` 直接读 | Qt6 内建 PNG/JPG/BMP，无需 libpng |
| PNG 序列 | `QDir` 自然排序 + `QImage` | 简单 |
| WebP 动图 | **延后**（需 vcpkg libwebp） | 优先级低于 GIF/APNG，M1 末尾有余力再做 |

> APNG 拆帧技巧：扫描到 `acTL` 后，对每个 `fcTL`+`fdAT` 组合，
> 拼出 `PNG签名 + IHDR + (其他前置chunk) + 该帧IDAT数据 + IEND` 的完整字节流交给 `QImage::loadFromData`。
> `fdAT` 去掉头部 4 字节序号即 IDAT payload，需重算 CRC32（zlib 的 `crc32` 由 Qt 的 `qCompress` 不直接暴露，
> 需要手写一份 ~30 行的 CRC32 表实现，或链接 zlib）。

**统一产物**：

```cpp
struct Frame { int w, h; QByteArray rgba8; };   // 统一为画布尺寸，straight alpha

struct FrameSequence {
    int  width = 0, height = 0;
    std::vector<Frame>  frames;
    std::vector<double> delays;      // 秒
    double totalDuration = 0;        // 0 表示静态图
    bool   isStatic() const { return frames.size() <= 1; }
    int    frameIndexAt(double t) const;
    size_t bytes() const;            // 用于内存预算检查
};
```

**合成器共用**（GIF 与 APNG 的 disposal 语义完全同构，别写两遍）：

```cpp
class CanvasCompositor {
    // op: 0=NONE 1=BACKGROUND(清该帧矩形) 2=PREVIOUS(恢复绘制前备份)
    void blend   (const Frame& src, int x, int y, int blendOp);  // 0=SOURCE 1=OVER
    void dispose (int disposeOp, const QRect& frameRect);
};
```

**GIF 必须踩对的点**（残影 100% 来自这里）：

| 项 | 规则 |
|---|---|
| disposal | 0/未指定 → 视同 1（保留）；1 保留；2 绘制后把该帧矩形填背景；3 恢复绘制前备份 |
| 透明 | `transparent color flag` + index；GIF 只有 1bit alpha |
| delay | 单位 1/100 秒；**≤ 1cs 的畸形值兜底为 10cs**（否则浏览器级素材会闪成幻灯片） |
| 调色板 | 局部调色板优先于全局；局部可带 `sort`/`size` |
| 交织 | `interlace flag` 需按 4-pass 解交织 |
| 边界 | 帧矩形可能越界 → clamp 到画布 |
| 首帧前 | 画布先填背景色（或全透明） |
| LZW | clear code 后字典重置；code size 增长到 12 后需处理"字典满但不 clear"的畸形流 |

**内存预检**（防止一个 200MB 的 GIF 直接吃满）：
```
估算 = w * h * 4 * frames
  > 150MB  → 弹框提示 + 自动等比缩小到长边 512
  > 60MB   → 仅提示
```

### 12.2 渲染层改造（`src/render/`）

把 M0 的"程序化圆盘"替换为"精灵播放"，其余（上下文、shader、stencil 预留）不动。

- **纹理策略：单张 RGBA 纹理 + `glTexSubImage2D` 增量更新**
  不为每帧建一个 texture（100 帧 × 512² 会让 GPU 内存翻倍）。
  帧变化时只更新纹理内容；帧未变化时**一次 GL 调用都不发**。
- **着色器**：复用 M0 的顶点/片元，把"按距离算 alpha"换成 `texture2D(tex, uv)`。
  stencil 相关代码保留注释，M2 虹膜裁剪直接接上。
- **尺寸变化**：素材尺寸 ≠ 窗口尺寸时，等比缩放并 `resize` 窗口（保持 DPR 换算）。
- 帧数据统一为画布尺寸，避免每帧不同尺寸导致的 texImage2D 重建。

**帧调度 + 三重短路**（这是 M1 省 CPU 的全部来源）：

```
1. 静态图（frames <= 1）
     → 上传一次后 **直接停掉动画定时器**，CPU 归零
2. idx == lastFrameIndex
     → 直接 return，不上传纹理、不 update()、不绘制
3. 窗口不可见 / 最小化 / 被全屏遮挡
     → 停掉动画定时器与光标轮询定时器
```

> 定时器间隔保持 16ms（`Qt::PreciseTimer`），不按素材帧率改——改了会引入抖动。
> 省电靠上面的短路，不靠降频。

### 12.3 应用层（`src/app/`）

| 功能 | 实现 | 备注 |
|---|---|---|
| 加载素材 | 托盘菜单"打开素材…" + **拖文件到窗口** | `QFileDialog` + `dragEnterEvent` |
| 拖拽移动 | M0 已有 | 拖拽结束写回 QSettings |
| 缩放 | 右键菜单 / 托盘：25%~400% | 用整数倍优先，避免采样糊 |
| 透明度 | 10%~100% | `setWindowOpacity` |
| 置顶 | 开关 | Windows 保留 2s 看门狗（会被全屏窗口抢走） |
| 开机自启 | 三平台 `#ifdef` | Windows 写 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`；macOS 写 `~/Library/LaunchAgents/*.plist`；Linux 写 `~/.config/autostart/*.desktop`。**后两者只保证能编译** |
| 配置持久化 | `QSettings`（`QSettings::IniFormat`，路径可控） | 位置、缩放、透明度、置顶、最近 5 个素材 |
| 托盘 | `QSystemTrayIcon` | 三平台一致；不可用时降级为仅右键菜单 |

### 12.4 文件清单与工作量

| 文件 | 行数估算 | 说明 |
|---|---|---|
| `media/FrameSequence.h/.cpp` | ~150 | 数据结构 + `frameIndexAt` + 内存估算 |
| `media/CanvasCompositor.h/.cpp` | ~120 | blend / dispose，GIF 与 APNG 共用 |
| `media/GifDecoder.h/.cpp` | ~450 | LZW + 调色板 + 交织 + disposal，**最重的一块** |
| `media/ApngDecoder.h/.cpp` | ~250 | chunk 扫描 + 拆帧 + CRC32 + QImage |
| `media/SequenceLoader.h/.cpp` | ~120 | 目录序列 + 静态图 + 自然排序 |
| `media/MediaLoader.h/.cpp` | ~100 | 按魔数嗅探分发（不看扩展名，更稳） |
| `render/SpriteRenderer.h/.cpp` | ~220 | 纹理 + 帧调度 + 三重短路 |
| `app/Settings.h/.cpp` | ~150 | QSettings 封装 |
| `app/AutoStart.h/.cpp` | ~120 | 三平台 `#ifdef` |
| `app/PetController` 改造 | ~200（改） | 接入播放、拖拽加载、菜单 |
| `src/media/CMakeLists.txt` | — | 启用 media 子目录（M0 已注释） |
| **合计** | **≈ 1900 行新增 + 200 行改造** | |

**工作量**：解码层 3–4 人日（GIF 占一半）· 渲染改造 1.5 人日 ·
应用层 2 人日 · 联调与优化 2 人日 ≈ **8–9 人日**。

### 12.5 验收标准

1. **GIF 正确性**：准备 3 个素材——纯 disposal=1、含 disposal=2（区域清空）、含 disposal=3（恢复备份）。
   播放**无残影、无闪烁、颜色与浏览器一致**。
2. **APNG**：至少 1 个带 `blend_op=OVER` 的素材播放正确。
3. **静态图**：加载后 `renderFps` 归零、动画定时器停止、`cpu` 显示 ≈ 0。
4. **性能**：24fps / 320² / 60 帧的 GIF，`renderFps` 稳定在 **24 左右而非 60**（证明短路生效），CPU < 1.5%。
5. **内存**：上述素材常驻 ≤ 120MB；拖入一个 200MB 的超大 GIF 时触发降采样提示而不是直接吃满。
6. **交互**：拖动正常；透明区能穿透点到下层窗口；右键菜单与托盘功能齐全。
7. **持久化**：退出重进后位置、缩放、透明度、素材都恢复。
8. **穿透稳定性**：静止不动 30 秒，`toggles` 计数**不增长**（M0 里 Qt 后端抖过一次，M1 必须锁死为 native）。

### 12.6 M1 风险

| 风险 | 影响 | 对策 |
|---|---|---|
| GIF LZW 边界（code size 增长、字典满未 clear） | 花屏或死循环 | 解码加迭代上限；准备 10+ 个畸形/边缘素材做覆盖测试 |
| APNG 拆帧的 CRC32 算错 | `QImage` 直接返回空 | 先做 1 个基准素材的 golden test，比对每帧像素 |
| disposal=3 的整帧备份导致瞬时内存翻倍 | 加载峰值冲高 | 只备份**帧矩形区域**而非整画布 |
| 超大素材内存失控 | OOM | 加载前预检 + 降采样，见 12.1 |
| 拖放文件格式嗅探误判 | 加载失败 | 魔数优先，扩展名兜底，失败给明确错误提示 |

---

## 13. M1 实现结果（2026-09-18，Windows 已实测）

M1 代码已全部落地并跑通。**零第三方依赖**——GIF 完全自研，APNG 只做 chunk 层，
IDAT 交给 Qt 内置 PNG 解码器。共新增约 1900 行。

### 13.1 实测数据（test.gif：240x240 / 24 帧 / 全部 disposal=2 / 100ms 每帧 / 循环）

| 场景 | renderFps | 常驻内存 | CPU（1 秒采样） |
|---|---|---|---|
| **GIF 播放** | **10**（= 素材帧率，而非 60） | 84.5 MB | 0 – 6.3%，典型 1.6% |
| **静态图** | **0**（首帧后完全停止重绘） | 70.4 MB | 0 – 1.6% |
| **目录序列（24 帧）** | 10 | 84.5 MB | 0 – 4.6% |
| 无素材（M0 圆盘） | 62 | 70 MB | 1.6 – 10.9% |

**关键结论：GIF 场景 `renderFps` 稳定在 10 而不是 60 —— 三重短路生效的直接证据。**
待机（静态图）时 `timerFps=0 renderFps=0`，动画定时器已停，只剩 Qt 事件循环开销。

### 13.2 正确性验证：逐像素比对

新增工具 `tools/mediadump`：把素材每一帧导出为 PNG，再用 `temp/compare.py`
与 PIL 的合成结果逐像素比对（PIL 是业界公认的 GIF disposal 参考实现）。

```
RESULT: ALL FRAMES MATCH (alpha + visible RGB)   # 24/24 帧
```

- `alphaDiff = 0`：**每帧每个像素的 alpha 完全一致** → disposal=2 的清屏语义实现正确，无残影
- `rgbDiff(visible) = 0`：不透明像素的颜色完全一致 → LZW、调色板、透明索引都正确
- 唯一差异在 alpha=0 区域的颜色值（PIL 保留绿幕色 `(0,255,0)`，本实现保留 `(0,0,0)`），
  因 alpha 为 0 故视觉不可见，比对脚本对此单独统计、不计入失败

渲染路径另用 `--grab <png>` 离屏抓取验证（不依赖窗口是否可见）：
抓到的 450x450 帧透明区占比 84.1%，不透明内容包围盒换算回画布坐标后与参考帧
的 alpha 包围盒吻合到 1px 以内。

### 13.3 三处与原规划的偏离（均为实测后的决定）

1. **内存目标进一步放宽**：GIF 场景实测 84.5 MB（素材帧仅 5.3 MB，其余是 Qt 模块 +
   GPU 驱动 + dpr 1.5 的 450x450 后台缓冲）。原定"单只 ≤ 120MB"仍然满足，但要注意
   首次运行冷启动会出现约 150MB 的瞬时峰值（Qt/驱动一次性缓存），后续运行回落到 ~85MB。
2. **光标轮询 16ms → 32ms**：实测 16ms 时静态图仍有 ~1.6% 的持续 CPU。
   降到 30Hz 并加上"光标没动就不重新判定"的去重后，待机时几乎不做任何工作。
   > 去重有个坑：拖拽结束 / 菜单关闭时必须**主动失效缓存**，否则光标不动就永远不会
   > 重新判定，角色会一直保持"不穿透"状态。
3. **`--grab` 离屏抓取**：原本打算靠全屏截图验证渲染，实测发现**全屏独占游戏会盖住
   置顶的桌宠窗口**（置顶看门狗无效）。改用 `QOpenGLWidget::grabFramebuffer()` 离屏抓取，
   与窗口可见性解耦，也更适合做自动化回归。

### 13.4 M1 遗留

| 项 | 状态 |
|---|---|
| disposal=3（恢复备份） | 代码已实现，**手上没有素材覆盖测试** |
| APNG | 代码已实现（含手写 CRC32 + 拼帧），**完全未测** |
| 交织（interlace）GIF | 代码已实现，未测 |
| macOS / Linux | 按约定不做实测，只保证代码层可编译 |
| 开机自启 | 三平台实现都写了，只有 Windows 路径实测 |

### 13.5 M1 新增文件

```
src/media/        FrameSequence / CanvasCompositor / GifDecoder / ApngDecoder / MediaLoader
src/render/       PetRenderer 增加精灵模式（单纹理 + glTexSubImage2D 增量更新）
src/app/          Settings（QSettings）、AutoStart（三平台 ifdef）、
                  PetController 接入播放与三重短路、PetWindow 支持拖放
tools/mediadump/  帧导出工具，用于逐像素回归
```
